#include <bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL)
#define endl '\n'

using namespace std;

int main() {
  fast;
  int n;

  while (cin >> n) {
    if (n) cout << "vai ter duas!" << endl;
    else   cout << "vai ter copa!" << endl;
  }

  return 0;
}
