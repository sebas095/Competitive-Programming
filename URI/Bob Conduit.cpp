#include <bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL)
#define endl '\n'

using namespace std;

typedef long long ll;

int main() {
  fast;
  int t;
  ll a, b;
  cin >> t;

  while (t--) {
    cin >> a >> b;
    cout << a + b << endl;
  }

  return 0;
}
