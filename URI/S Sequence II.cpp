#include <bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL)
#define endl '\n'

using namespace std;

int main() {
  fast;
  // double s = 0;
  // for (int i = 1, j = 0; i <= 39; i += 2, j++) s += i / (double)(1 << j);
  cout << fixed << setprecision(2) << 6.0 << endl;
  return 0;
}
